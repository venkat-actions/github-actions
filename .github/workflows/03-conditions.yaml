name: Conditions
on: [push]
jobs:
  hello:
    runs-on: ubuntu-latest 
    steps:
      - name: step1 
        run: echo "Hello World"
      - name: step2 
        if: github.ref == 'refs/heads/main'
        run: echo "Hello GitHub Actions"